# Evidence Patterns

Use concise evidence summaries:

- `Result`: what was delivered
- `Artifact proof`: where the output exists
- `Goal proof`: how it matches the target
- `Gap`: what failed if acceptance is incomplete

Prefer direct proof over explanation. If one screenshot, one path, or one read-back state proves the point, use that instead of a long narrative.
